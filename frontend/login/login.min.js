const form=document.querySelector("form");function authentication(username,password){return"admin"===username&&"admin"===password}form.addEventListener("submit",(e=>{e.preventDefault();authentication(form.username.value,form.password.value)?window.location.href="display.html":alert("wrong")}));